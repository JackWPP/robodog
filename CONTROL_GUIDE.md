# 机器狗增强控制系统使用说明

本项目为机器狗提供了三种增强的控制方式，实现了即按即走、调速、调节身高等功能。

## 安装依赖

首先安装所需的Python包：

```bash
pip install -r requirements.txt
```

## 控制方式

### 1. 改进版键盘控制 (improved_keyboard_control.py)

**特点：**
- 即按即走，松开即停
- 多档速度调节 (6档)
- 多档身高调节 (5档)
- 实时状态显示

**使用方法：**
```bash
python examples/improved_keyboard_control.py
```

**控制说明：**
- **移动控制（即按即走）:**
  - `W / ↑`: 前进
  - `S / ↓`: 后退
  - `A / ←`: 左移
  - `D / →`: 右移
  - `Q`: 左转
  - `E`: 右转
  - `空格`: 紧急停止

- **速度调节:**
  - `+ / =`: 提高速度
  - `- / _`: 降低速度

- **高度调节:**
  - `Z`: 提高身高
  - `X`: 降低身高

- **程序控制:**
  - `R`: 重置姿态
  - `ESC`: 退出程序

### 2. 图形界面控制 (robodog_gui.py)

**特点：**
- 直观的图形界面
- 支持键盘和鼠标操作
- 实时状态监控
- 滑块调速和调高
- 按钮式移动控制

**使用方法：**
```bash
python examples/robodog_gui.py
```

**界面功能：**
- **连接控制**: 输入机器狗IP地址并连接
- **速度控制**: 使用滑块调节速度等级 (1-6档)
- **身高控制**: 使用滑块调节身高等级 (1-5档)
- **移动控制**: 
  - 键盘控制：`WASD`移动，`QE`转向
  - 按钮控制：点击界面上的移动按钮
- **功能按钮**: 紧急停止、重置姿态
- **状态显示**: 实时显示连接状态、速度、身高等信息

### 3. 游戏手柄控制 (gamepad_control.py)

**特点：**
- 支持Xbox/PlayStation等标准游戏手柄
- 模拟摇杆精确控制
- 按钮快捷功能

**使用方法：**
```bash
python examples/gamepad_control.py
```

**手柄控制说明：**
- **左摇杆**: 前后左右移动
- **右摇杆**: 左右旋转
- **Y按钮**: 升高身体
- **A按钮**: 降低身体
- **B按钮**: 紧急停止
- **X按钮**: 重置姿态
- **START按钮**: 退出程序

### 4. 游戏手柄图形界面控制 (gamepad_control_gui.py)

**特点：**
- 图形界面显示手柄连接状态
- 实时参数调节 (速度、转向、死区)
- 手柄连接引导和状态监控
- 实时状态显示和日志记录

**使用方法：**
```bash
python examples/gamepad_control_gui.py
```

**界面功能：**
- **手柄状态区域**: 
  - 显示手柄连接状态和名称
  - 提供手柄连接指导
  - 刷新手柄连接功能
- **机器狗连接**: 输入IP地址并连接机器狗
- **控制参数调节**:
  - 最大速度滑块 (0.5-3.0 m/s)
  - 转向速度滑块 (0.3-2.0 rad/s) 
  - 摇杆死区滑块 (0.0-0.3)
- **实时状态监控**: 显示速度、身高、手柄状态等
- **控制按钮**: 开始/停止控制，清空日志

**手柄控制说明：**
- **左摇杆**: 前后左右移动
- **右摇杆**: 左右旋转
- **Y按钮**: 升高身体
- **A按钮**: 降低身体
- **B按钮**: 紧急停止
- **X按钮**: 重置姿态
- **START按钮**: 停止控制

## 配置说明

### IP地址配置

在使用任何控制程序前，请确保修改代码中的机器狗IP地址：

```python
host = '192.168.118.29'  # 修改为您的机器狗实际IP地址
```

### 速度和高度参数

**速度等级 (m/s):**
1. 0.2 - 慢速
2. 0.5 - 较慢
3. 0.8 - 中等 (默认)
4. 1.2 - 较快
5. 1.5 - 快速
6. 2.0 - 最快

**身高等级 (m):**
1. 0.15 - 最低
2. 0.20 - 较低
3. 0.25 - 中等 (默认)
4. 0.30 - 较高
5. 0.35 - 最高

## 安全注意事项

1. **网络连接**: 确保机器狗和控制设备在同一网络中
2. **紧急停止**: 所有控制方式都提供紧急停止功能
3. **程序退出**: 程序退出时会自动重置机器狗姿态
4. **环境安全**: 在开阔安全的环境中操作机器狗

## 故障排除

### 连接问题
- 检查机器狗是否开机
- 确认IP地址是否正确
- 检查网络连接

### 键盘控制无响应
- 确保程序窗口获得焦点
- 检查是否安装了pynput库

### 手柄控制无响应
- 确认手柄已正确连接
- 检查是否安装了pygame库
- 尝试重新插拔手柄

### GUI界面问题
- 确保tkinter库可用 (Python标准库)
- 检查是否有其他程序占用窗口焦点

## 扩展功能

### 自定义速度等级
修改代码中的`speed_levels`数组：
```python
self.speed_levels = [0.1, 0.3, 0.6, 1.0, 1.4, 1.8]  # 自定义速度
```

### 自定义身高等级
修改代码中的`height_levels`数组：
```python
self.height_levels = [0.12, 0.18, 0.23, 0.28, 0.33]  # 自定义身高
```

### 添加新的控制键
在`movement_keys`字典中添加新的按键映射：
```python
self.movement_keys = {
    # ...现有映射...
    'i': {'vx': 0.5, 'vy': 0.5, 'wz': 0.0, 'name': '右前移动'},  # 新增
}
```

## 开发说明

项目结构：
- `improved_keyboard_control.py`: 改进的键盘控制
- `robodog_gui.py`: 图形界面控制
- `gamepad_control.py`: 游戏手柄控制

所有控制程序都基于相同的控制逻辑，可以根据需要进行定制和扩展。
